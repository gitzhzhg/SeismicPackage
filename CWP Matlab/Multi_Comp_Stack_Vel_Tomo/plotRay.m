%==============================================================================
% Plot model interfaces, positions of sources and receivers
%==============================================================================

function [] = plotRay(figInterfaceall);

global p1 p2 q ugr vgr xray tau time

%==============================================================================
figure(figInterfaceall);
plot3(xray(1,:), xray(2,:), xray(3,:), 'k-', 'LineWidth', 2);

%==============================================================================

