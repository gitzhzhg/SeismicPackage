Fimpulse	computes prestack, DMO, or poststack impulse
		responses (short test)

Fowler		computes a long SUTIFOWLER test (about 6 1/2 hr
		on IBM 530) on a fan-like synthetic model
		with 31 offsets.

Fztest		computes a short SUTIFOWLER test on one offset
		from a fan-like synthetic model.

Plot		displays selected output from the Fowler
		test job.


Type shell script name to execute.
