// This empty do-nothing file exists in order to make the Makefile work correctly.
// If there is a native C++ program, but no native utilities, in the directory, the
// Makefile mysteriously appears to use a built-in C++ compiler command which does
// not have the compiler flags (and the include files) to compile the native program.

void empty_do_nothing() {}
