!!------------------------ c4we_wrap.f90 --------------------------!!
!!------------------------ c4we_wrap.f90 --------------------------!!
!!------------------------ c4we_wrap.f90 --------------------------!!

!!!!!!!!!! DO NOT EDIT THIS FILE - it is machine generated !!!!!!!!!!

!!------------------------------ module -----------------------------------!!
!!------------------------------ module -----------------------------------!!
!!------------------------------ module -----------------------------------!!

      module c4we_wrap_module
      use c4we_module
      implicit none
      public

      type :: c4we_wrap_struct
        type(c4we_struct),pointer :: obj
      end type c4we_wrap_struct

      end module c4we_wrap_module

!!---------------------------- create ------------------------------------!!
!!---------------------------- create ------------------------------------!!
!!---------------------------- create ------------------------------------!!

      subroutine c4we_wrap_create (fpoint)  
      use c4we_wrap_module
      implicit none
      type(c4we_wrap_struct),intent(out)   :: fpoint
      type(c4we_struct)     ,pointer       :: obj           ! local

      nullify (obj)               ! needed for intel compiler.
      call c4we_create (obj)
      fpoint%obj => obj

      end subroutine c4we_wrap_create

!!---------------------------- delete ------------------------------------!!
!!---------------------------- delete ------------------------------------!!
!!---------------------------- delete ------------------------------------!!

      subroutine c4we_wrap_delete (fpoint)   
      use c4we_wrap_module
      implicit none
      type(c4we_wrap_struct),intent(inout) :: fpoint
      type(c4we_struct)     ,pointer       :: obj           ! local

      obj => fpoint%obj
      if (.not. associated(obj)) return
      call c4we_delete (obj)
      fpoint%obj => obj

      end subroutine c4we_wrap_delete

!!---------------------------- update ------------------------------------!!
!!---------------------------- update ------------------------------------!!
!!---------------------------- update ------------------------------------!!

      subroutine c4we_wrap_update (fpoint)     
      use c4we_wrap_module
      implicit none
      type(c4we_wrap_struct),intent(inout) :: fpoint
      type(c4we_struct)     ,pointer       :: obj           ! local

      obj => fpoint%obj
      if (.not. associated(obj)) return
      call c4we_update (obj)

      end subroutine c4we_wrap_update

!!---------------------------- wrapup ------------------------------------!!
!!---------------------------- wrapup ------------------------------------!!
!!---------------------------- wrapup ------------------------------------!!

      subroutine c4we_wrap_wrapup (fpoint)      
      use c4we_wrap_module
      implicit none
      type(c4we_wrap_struct),intent(inout) :: fpoint
      type(c4we_struct)     ,pointer       :: obj           ! local

      obj => fpoint%obj
      if (.not. associated(obj)) return
      call c4we_wrapup (obj)

      end subroutine c4we_wrap_wrapup

!!---------------------------- oneset ------------------------------------!!
!!---------------------------- oneset ------------------------------------!!
!!---------------------------- oneset ------------------------------------!!

      subroutine c4we_wrap_oneset (fpoint,ntr,  &
                                   hd,tr,lenhd,lentr,num)
      use c4we_wrap_module
      implicit none
      type(c4we_wrap_struct),intent(inout) :: fpoint
      integer               ,intent(in)    :: lenhd,lentr,num  ! argument
      integer               ,intent(inout) :: ntr              ! argument
      double precision      ,intent(inout) :: hd(lenhd,num)    ! argument
      real                  ,intent(inout) :: tr(lentr,num)    ! argument
      type(c4we_struct)     ,pointer       :: obj              ! local

      obj => fpoint%obj
      if (.not. associated(obj)) return
      call c4we (obj,ntr,hd,tr)                ! might be commented out.

      end subroutine c4we_wrap_oneset

!!---------------------------- twosets ------------------------------------!!
!!---------------------------- twosets ------------------------------------!!
!!---------------------------- twosets ------------------------------------!!

      subroutine c4we_wrap_twosets (fpoint,ntr,                 &
                                    hd1,tr1,lenhd1,lentr1,num1, &
                                    hd2,tr2,lenhd2,lentr2,num2)
      use c4we_wrap_module
      implicit none
      type(c4we_wrap_struct),intent(inout) :: fpoint
      integer               ,intent(in)    :: lenhd1,lentr1,num1 ! argument
      integer               ,intent(in)    :: lenhd2,lentr2,num2 ! argument
      integer               ,intent(inout) :: ntr                ! argument
      double precision      ,intent(inout) :: hd1(lenhd1,num1)   ! argument
      real                  ,intent(inout) :: tr1(lentr1,num1)   ! argument
      double precision      ,intent(inout) :: hd2(lenhd2,num2)   ! argument
      real                  ,intent(inout) :: tr2(lentr2,num2)   ! argument
      type(c4we_struct)     ,pointer       :: obj                ! local

      obj => fpoint%obj
      if (.not. associated(obj)) return
!!!!  call c4we (obj,ntr,hd1,tr1,hd2,tr2)      ! might be commented out.

      end subroutine c4we_wrap_twosets

!!----------------------------- end ---------------------------------------!!
!!----------------------------- end ---------------------------------------!!
!!----------------------------- end ---------------------------------------!!
