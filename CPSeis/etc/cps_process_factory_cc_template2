
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <assert.h>

#define DOIT(proc,wrapper)  if(strcmp(name, proc) == 0) return new wrapper();
#define CATE(proc,category) if(strcmp(name, proc) == 0) return category;
#define PURP(proc,purpose)  if(strcmp(name, proc) == 0) return purpose;


//------------------------------ build ----------------------------//
//------------------------------ build ----------------------------//
//------------------------------ build ----------------------------//


CpsBaseWrapper *CpsProcessFactory::build(const char *name)
{
